apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://servly-prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 15s
      prometheusVersion: latest
    version: 1

